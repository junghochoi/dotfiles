return {
	"folke/trouble.nvim",
	opts = {

		auto_preview = false,
		auto_fold = true,
		fold_open = "", -- icon when folded
		fold_closed = "", -- icon when closed
	}, -- for default options, refer to the configuration section for custom setup.
	cmd = "Trouble",
}
