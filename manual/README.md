# Open-app script for KDE Plasma


```bash

yay -S kdotool
sudo cp ~/dotfiles/manual/kde-plasma/open-app /bin
sudo chmod +x /bin/open-app

# Custom instructions specificially for google-chromej
sudo ln -s /usr/bin/google-chrome-stable /usr/local/bin/google-chrome
```


